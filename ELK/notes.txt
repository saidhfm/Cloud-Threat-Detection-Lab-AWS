#first deploy cribil on same elk instance then login http://pip:9000 to access via web
 #then configure source -> elasticsearch->add new> give elastic-input-9200 -> 0.0.0.0->save
 #now in elastic -> fleet -> settings -> in outputs -> add another url add -> http://private ip of elk :9111
 #now deploy splunk in agent machine -> 
 #now in cribil select add destination splunk-> name splunkoutput -> ip is public ip of splunk(deployed on endpoint) http://pubip, port is same no change save
 #now open splunk search for *
 #new relic -> login-> add integrations -> skip-> on right side -> generate and copy license key->
 #now in cribil-> add destination->newrelic-events>outputid(newrelicoutput)->api key -> paste the above copied license key from new relic-.account-> u can find in url of new relic->event time -> endpointlogs->next->save
 #100gb of data is free for month
 #https://github.com/peasead/elastic-container/
